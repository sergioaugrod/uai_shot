language: elixir

stages:
  - test
  - quality

elixir: 1.9
otp_release: 22

jobs:
  include:
    - stage: test
      script: mix test

    - stage: quality
      script: mix format --check-formatted

    - stage: quality
      script: mix credo --strict
